import Level from '@engine/level/level';
import LevelTrait from '@engine/level/level-trait';

export default class LevelCollider implements LevelTrait {
    update(level: Level): void {
        // TODO LevelCollider
    }
}
